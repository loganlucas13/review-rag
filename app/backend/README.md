# Backend

> [!WARNING]
> WIP

-   Uses [Flask](https://flask.palletsprojects.com/en/stable/).

## Instructions

1. Make sure all required packages are installed (follow steps in root directory readme)
2. Run `main.py` using Python3
    - Mac/Linux: `python3 main.py`
    - Windows: `py -3 main.py`
